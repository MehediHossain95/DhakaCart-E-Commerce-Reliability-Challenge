# --- General/OS files ---
.DS_Store
Thumbs.db

# --- Node/JavaScript ---
/node_modules
/backend/node_modules
/frontend/node_modules
npm-debug.log*
yarn-debug.log*

# --- Terraform & AWS Artifacts ---
# The state file contains sensitive outputs (like the public IP)
/terraform/terraform.tfstate
/terraform/terraform.tfstate.backup
# The directory containing provider binaries
/terraform/.terraform/
# Terraform variable files (often contain secrets/credentials)
/terraform/*.tfvars
/terraform/*.tfvars.json

# --- Docker ---
# Ignore built images/containers if you generate them locally
# *.img

# --- Secrets and Keys ---
# CRITICAL: DO NOT COMMIT YOUR PRIVATE KEY
dhakacart-key
